classDiagram
    class User {
        -int userId
        -String name
        -String email
        -String password
        -String role
        +login()
        +logout()
    }
    class Manager {
        -String role="manager"
        +generateReport()
    }
    class Pharmacist {
        -String licenseNumber
        +dispenseMedication()
    }
    class Patient {
        -int patientId
        -String name
        -String phone
        -String insuranceId
        +getPatient()
        +addPatient()
    }
    class Medication {
        -int medicationId
        -String name
        -float price
        -boolean requiresPrescription
        +getMedication()
        +updateStock()
    }
    class Category {
        -int categoryId
        -String name
        +getCategory()
    }
    class Supplier {
        -int supplierId
        -String name
        -String contact
        +getSupplier()
    }
    class Prescription {
        -int prescriptionId
        -String doctorName
        -Date dateIssued
        +verifyPrescription()
    }
    class Sale {
        -int saleId
        -Date date
        -float totalAmount
        +createSale()
        +calculateTotal()
    }
    class SaleItem {
        -int saleItemId
        -int quantity
        -float subtotal
        +calculateSubtotal()
    }

    User <|-- Manager
    User <|-- Pharmacist

    Category "1" -- "*" Medication : +contains
    Supplier "1" -- "*" Medication : +supplies
    Patient "1" -- "*" Prescription : +owns
    Prescription "*" -- "*" Medication : +includes
    Pharmacist "1" -- "*" Sale : +processes
    Patient "1" -- "*" Sale : +makes
    Sale "1" *-- "1..*" SaleItem : +has
    SaleItem "*" --> "1" Medication : +references